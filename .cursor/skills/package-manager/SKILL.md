---
name: package-manager
description: Rules on what package manager
---

# Package manager

This project is using Bun. Use Bun cli commands in the terminal instead of NPM every time, with no exceptions. Using NPM commands will not work and thus cannot be used.